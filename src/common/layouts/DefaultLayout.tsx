import styled from "@emotion/styled";

/**
 * Simple div for now, but this is where we can evolve a common layout that might include
 * shared page elements such as header, sidebar, footer, etc.
 */
export const DefaultLayout = styled.div`
  padding: 20px 45px;
`